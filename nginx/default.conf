server {
        listen 80 default_server;
        listen [::]:80 default_server;

        location / {
                proxy_pass http://ocr:8000;
        }

        location /static {
                alias /static;
        }

        location /media {
                alias /media;
        }
}
